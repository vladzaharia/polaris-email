{
  "name": "@polaris-email/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20240502.0"
  }
}
