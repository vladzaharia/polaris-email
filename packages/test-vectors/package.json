{
  "name": "@polaris-mail/test-vectors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "tsx src/generate.ts > vectors.json && echo wrote vectors.json",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@polaris-mail/hmac": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
