# @polaris-mail/sdk (Node)

Full documentation lives at https://docs.mail.plrs.im/developers/sdks/node.

For local development inside the polaris-mail monorepo, this package
publishes the same exports the docs describe; see `src/` for the
sources.
