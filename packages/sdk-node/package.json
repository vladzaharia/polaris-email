{
  "name": "@polaris/sdk",
  "version": "0.1.0",
  "//private": "Internal-only within the polaris-* service family (see LICENSE). Not published to npm; consumed via pnpm workspace by apps/panel and services/*. Removing `private` requires a LICENSE change and a publish workflow (publishConfig, repository, npm credentials).",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./webhook": "./src/webhook.ts",
    "./node": "./src/node.ts",
    "./react": "./src/react.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@polaris-mail/hmac": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
