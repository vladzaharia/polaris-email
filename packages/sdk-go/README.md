# polaris-sdk-go

Full documentation lives at https://docs.mail.plrs.im/developers/sdks/go.

For local development inside the polaris-mail monorepo, this package
publishes the same exports the docs describe; see the `.go` sources at
the package root.
