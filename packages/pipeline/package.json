{
  "name": "@polaris-email/pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "echo ok",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@polaris-email/ids": "workspace:*",
    "@polaris-email/mime": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0"
  }
}
