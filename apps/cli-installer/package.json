{
  "name": "@polaris-mail/cli-installer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.server.json --noEmit",
    "lint": "oxlint .",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "polaris-mail setup infra deploy service cli-installer"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.4",
    "@cloudflare/workers-types": "^4.20260517.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.92.0"
  }
}
