// Template merged on top of wrangler.jsonc by `polaris-email setup infra
// deploy`. Placeholders use Go text/template syntax and are substituted
// by the Go renderer in apps/polaris-cli/internal/setup/wranglercfg from
// .deploy-state.json + .env.deploy. Do NOT hand-edit the materialized
// wrangler.local.jsonc — re-run `polaris-email setup infra render` instead.
{
  "account_id": "cf-account-id",
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "polaris-email",
      "database_id": "d1-id",
    },
  ],
  "r2_buckets": [{ "binding": "R2", "bucket_name": "polaris-email", "jurisdiction": "eu" }],
  "kv_namespaces": [
    { "binding": "KV_NONCE", "id": "kv-nonce-id" },
    { "binding": "KV_IDEMPOTENCY", "id": "kv-idem-id" },
    { "binding": "KV_RATE_LIMIT", "id": "kv-rl-id" },
    { "binding": "KV_KEY_CACHE", "id": "kv-kc-id" },
    { "binding": "KV_REVOCATIONS", "id": "kv-rev-id" },
  ],
  "vars": {
    "API_BASE_URL": "https://api.example.com",
    // R2 public custom domain. The R2 bucket `polaris-email` is fronted
    // by this hostname; both inbound and outbound message bodies/attachments
    // are served from it. The binding is provisioned during
    // `polaris-email setup infra apply` from R2_PUBLIC_HOST in .env.deploy.
    "R2_PUBLIC_HOST": "r2.example.com",
    "ALERT_WEBHOOK": "https://alerts.example.com/hook",
  },
}
