import { PageCard } from '../../layouts/PageCard.js';

export function WebhookSubsList() {
  return (
    <PageCard title="Webhook subscriptions" description="External + tailnet webhook targets.">
      <p className="text-sm text-[var(--color-muted-foreground)]">Phase I implements this view.</p>
    </PageCard>
  );
}
