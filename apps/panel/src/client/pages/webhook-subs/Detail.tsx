import { PageCard } from '../../layouts/PageCard.js';

export function WebhookSubDetail() {
  return (
    <PageCard title="Webhook subscription" description="Edit destination, retries, secret.">
      <p className="text-sm text-[var(--color-muted-foreground)]">Phase I implements this view.</p>
    </PageCard>
  );
}
