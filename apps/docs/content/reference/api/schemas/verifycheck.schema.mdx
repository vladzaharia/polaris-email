---
id: verifycheck
title: 'VerifyCheck'
description: 'One row in the verify response. `actual` carries free-form hint text'
sidebar_label: 'VerifyCheck'
hide_title: true
hide_table_of_contents: true
schema: true
sample: { 'name': 'string', 'ok': true, 'expected': 'string', 'actual': 'string' }
custom_edit_url: null
---

import Schema from '@theme/Schema';
import Heading from '@theme/Heading';

<Heading as={'h1'} className={'openapi__heading'} children={'VerifyCheck'}></Heading>

One row in the verify response. `actual` carries free-form hint text
in addition to the observed value so the verify flow can express
`missing` / `drift` / `unreachable` / `verified` states uniformly.

<Schema {...require('./verifycheck.Schema.json')}></Schema>
