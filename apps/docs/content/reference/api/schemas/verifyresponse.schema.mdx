---
id: verifyresponse
title: 'VerifyResponse'
description: 'Result of `POST /v1/admin/domains/:id/verify`. `status` reflects the'
sidebar_label: 'VerifyResponse'
hide_title: true
hide_table_of_contents: true
schema: true
sample:
  {
    'id': 'string',
    'status': 'pending',
    'checks': [{ 'name': 'string', 'ok': true, 'expected': 'string', 'actual': 'string' }],
    'message': 'string',
    'verified_at': 0,
  }
custom_edit_url: null
---

import Schema from '@theme/Schema';
import Heading from '@theme/Heading';

<Heading as={'h1'} className={'openapi__heading'} children={'VerifyResponse'}></Heading>

Result of `POST /v1/admin/domains/:id/verify`. `status` reflects the
DB-stored verification state after the run; `checks` is the per-record
breakdown the panel renders.

<Schema {...require('./verifyresponse.Schema.json')}></Schema>
