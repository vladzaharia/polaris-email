{
  "title": "Body",
  "body": {
    "required": true,
    "content": {
      "application/json": {
        "schema": {
          "type": "object",
          "required": ["mode"],
          "properties": {
            "mode": { "type": "string", "enum": ["planned", "emergency"] },
            "reason": { "type": "string", "maxLength": 500 },
            "new_label": { "type": "string" }
          },
          "title": "RotateRequest"
        }
      }
    }
  }
}
