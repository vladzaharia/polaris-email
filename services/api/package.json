{
  "name": "@polaris-email/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "oxlint .",
    "deploy": "../../bin/deploy.sh services/api"
  },
  "dependencies": {
    "@polaris-email/cf-api": "workspace:*",
    "@polaris-email/hmac": "workspace:*",
    "@polaris-email/ids": "workspace:*",
    "@polaris-email/mime": "workspace:*",
    "@polaris-email/pipeline": "workspace:*",
    "@polaris-email/providers": "workspace:*",
    "@polaris-email/revocation-do": "workspace:*",
    "@polaris-email/schema": "workspace:*",
    "hono": "^4.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
