{
  "name": "@polaris-email/in",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p test/integration/tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@polaris-email/arf-parser": "workspace:*",
    "@polaris-email/hmac": "workspace:*",
    "@polaris-email/ids": "workspace:*",
    "@polaris-email/mime": "workspace:*",
    "@polaris-email/pipeline": "workspace:*",
    "@polaris-email/suppressions": "workspace:*",
    "@polaris-email/tlsrpt-parser": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
