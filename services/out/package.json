{
  "name": "@polaris-email/out",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p test/integration/tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@polaris-email/ids": "workspace:*",
    "@polaris-email/mime": "workspace:*",
    "@polaris-email/policy-engine": "workspace:*",
    "@polaris-email/schema": "workspace:*",
    "@polaris-email/suppressions": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260517.1",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
