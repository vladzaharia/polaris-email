# File listing commit SHAs to ignore in `git blame`.
#
# Convention: add a SHA on its own line, preceded by a comment describing why
# (typically large mechanical reformat / lint auto-fix commits that would
# otherwise pollute blame). Configure git locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Phase M: initial oxfmt format pass across the tree
# <sha-will-be-added-after-commit>
