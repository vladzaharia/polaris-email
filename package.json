{
  "name": "polaris-mail",
  "version": "0.1.0",
  "private": true,
  "description": "Managed email service for the polaris-* family. Cloudflare Workers + Docker bridge.",
  "workspaces": [
    "packages/*",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm test:unit && pnpm test:integration",
    "test:unit": "vitest --project unit run",
    "test:integration": "vitest --project in-workers --project out-workers --project api-workers run",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "check": "pnpm typecheck && pnpm lint && pnpm fmt:check"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.0",
    "@types/node": "^25.8.0",
    "lefthook": "^2.1.6",
    "oxfmt": "^0.49.0",
    "oxlint": "^1.65.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.98.0"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "//": "webpack >=5.106 breaks docusaurus build (facebook/docusaurus#11923). Pin docusaurus's webpack to the last working release. The scoped override only affects @docusaurus/core's resolution — other webpack consumers (vitest-pool-workers, etc.) are untouched. @types/react is pinned to 19 globally so apps/panel's React 19 ReactNode (which includes bigint) doesn't clash with Docusaurus's React 18 transitive types — Docusaurus tolerates the type-skew at build time.",
    "overrides": {
      "@docusaurus/core>webpack": "5.105.0",
      "@types/react": "^19.2.14",
      "@types/react-dom": "^19.2.3"
    },
    "neverBuiltDependencies-//": "postman-code-generators runs `yarn install` on its sub-codegens during postinstall. That fails on a yarn-less runner (CI) and isn't needed — docusaurus-theme-openapi-docs only consumes its `convert()` function, not the codegen sub-packages.",
    "neverBuiltDependencies": [
      "postman-code-generators"
    ]
  }
}
